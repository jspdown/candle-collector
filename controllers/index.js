'use strict';

const getTimeframes = require('./get-timeframes');
const getPairs = require('./get-pairs');
const getCandles = require('./get-candles');

module.exports = { getTimeframes, getPairs, getCandles };
